#include <iostream>
#include <cmath>
using namespace std;
int main () 
{
	int a, b, c, d, e;
	float avg, per, total;
	total=a+b+c+d+e;
	avg=(a+b+c+d+e)/5;
	per=((a+b+c+d+e)/500)*100;
	cout<< "total marks" << total << "average marks" << avg << "percentage" <<per;
	return 0;
}
